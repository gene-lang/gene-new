Build and activate a durable workspace-scoped plugin named project_audit. It must expose two tools: project_audit.scan and project_audit.last. scan accepts an object with paths (a list of relative filenames) and report_path (a relative Markdown output filename). Compose the existing fs.read and fs.write tools, declaring those tool dependencies. Read every supplied file, compute per-file and aggregate non-empty line counts and counts of lines containing TODO or FIXME, write a Markdown report through fs.write, and persist the latest successful summary in plugin state. last returns that saved summary without rereading files. Handle missing files as structured errors; never save a successful result when reading or writing failed. Keep plugin callbacks bounded and use host tool-request continuations for I/O. Use plugins.build to author it, inspect its installed contract, then actually call project_audit.scan on ["src/orders.gene", "src/report.gene"] with report_path "audit-report.md". Call project_audit.last and read audit-report.md back to verify it. Fix and replace the plugin if anything fails. Report its revision and verified counts; do not claim success from installation alone. You have read/write authority for this isolated project and state home.
